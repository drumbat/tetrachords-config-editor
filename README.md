# Tetrachords — SD-Config Editor (hosted)

Auto-published build of an internal tool. `index.html` is a self-contained
single file (app + React + fonts inlined); do not hand-edit it here — it is
overwritten on every publish from the private source repo.

Open it in **Chrome or Edge** (Web MIDI SysEx is required) and connect a
Tetrachords in SD-config boot mode. Served over HTTPS by GitHub Pages, which
satisfies the Web-MIDI secure-context requirement.
